name: Test Action with Email

on: [push]  # Triggers when you push code

jobs:
  test:
    runs-on: ubuntu-latest  # Uses GitHub's free Linux runner (kitchen station)
    steps:
      - name: 23f2003754@ds.study.iitm.ac.in  # Your required step name
        run: echo "Hello from GitHub Actions! Email step complete."
      
      - name: Another step
        run: echo "Workflow finished successfully!"
